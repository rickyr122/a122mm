<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme._122mm.Splash" parent="Theme.SplashScreen">
        <!-- real Android 12+ attrs -->
        <item name="android:windowSplashScreenBackground">@android:color/black</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/a122mm_logo</item>
        <!-- make the icon background fully transparent = no circle -->
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>

        <item name="postSplashScreenTheme">@style/Theme._122mm</item>
    </style>
</resources>

